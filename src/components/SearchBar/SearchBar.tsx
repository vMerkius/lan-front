import { useState } from "react";
import "./searchbar.scss";
const SearchBar = () => {
  return (
    <div className="search-bar">
      <h2>search bar</h2>
    </div>
  );
};

export default SearchBar;
